void main(List<String> args) async {
  // The console demo now lives in tests using in-memory repositories under
  // test/infrastructure. This main is intentionally minimal to keep the
  // library free from test-only infrastructure.
}

